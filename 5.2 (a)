#include <stdio.h>

int main() {
    int x, y;
    float result;

    printf("Enter the value of x: ");
    scanf("%d", &x);

    printf("Enter the value of y: ");
    scanf("%d", &y);

    result = (float) (x + y) / (x - y);

    printf("Result: %f\n", result);

    return 0;
}
